# TFLite phoneme classifier (Layer 5)

Place `phoneme_classifier.tflite` here (~50MB) when exported from CMU pronunciation dictionary pipeline.

Used for: Vel'nar vocal coach match % and correction feedback (L5.06).

Imperium only. Tending has no constructed language curriculum.
